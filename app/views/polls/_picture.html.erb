<%= simple_form_for @poll do |f| %>
  <%= f.input :photo, as: :attachinary %>
  <%= hidden_field_tag :extraparam, 0 %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<!--
$x("//*[@id='new_poll']/div[1]/div/input")["0"].defaultValue
 -->
